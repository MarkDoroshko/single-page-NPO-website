# Frontend

## Стек технологий
`pnpm`, `Astro + Islands Architecture`, `React`, `Prettier`, `sass`

## Основные команды
- `pnpm create astro@latest` - скачивает и запускает скрипт создания нового проекта Astro
- `pnpm i` - установка всех зависимостей проекта в текущей директории
- `pnpm astro add react` - автоматически:
  - Устанавливает `@astrojs/react`
  - Обновляет `astro.config.mjs`
  - Добавляет необходимые зависимости

## Преимущества pnpm в Astro:
- **Быстрее** установка зависимостей
- **Экономит место** на диске (hard links)
- **Строгая** установка зависимостей
- **Лучшая** организация node_modules

## Что такое Astro?
**Astro** — это современный фреймворк для построения быстрых сайтов. Его главная фишка — он отрисовывает страницы на сервере и присылает браузеру уже готовый HTML, что делает загрузку мгновенной.

## Архитектура Островов (Islands Architecture)
Это концепция, где страница состоит из независимых интерактивных "островков" в море статического HTML.

Каждый "остров" живет своей жизнью и загружается независимо.

## Базовая структура проекта Astro
```
frontend/
├── src/
│   ├── components/
│   │   ├── Header.astro
│   │   └── Counter.jsx
│   ├── layouts/
│   │   └── Layout.astro
│   └── pages/
│       └── index.astro
├── public/
│   └── favicon.ico
└── package.json
```

### `src/` - Исходный код проекта
- Здесь вся логика и компоненты сайта

### `src/components/` - Компоненты
- `Header.astro` - Astro-компонент (серверный, мало JS)

- `Counter.jsx` - React-компонент (клиентский, интерактивный)

- Можно смешивать: `.astro`, `.jsx`, `.vue`, `.svelte`

### `src/layouts/` - Шаблоны страниц
- `Layout.astro` - обертка для всех страниц (хедер, футер, стили)

- Переиспользуемая структура

### `src/pages/ `- Страницы сайта
- `index.astro` - главная страница (`/`)

- Автоматическая маршрутизация: `about.astro` → `/about`

### `public/` - Статические файлы
- `favicon.ico` - иконка сайта

- Изображения, шрифты, PDF - всё что нужно "как есть"

- Доступны по корневому пути: `/favicon.ico`

## 📄 Файлы
### `package.json` - Зависимости и скрипты
- Список пакетов (astro, react, etc.)
- Скрипты: `dev`, `build`, `preview`
